---
import site from '../site.json';
const SITE = site;
const { current = '' } = Astro.props;
const navItems = [
  { href: '/', label: '首页', key: 'home' },
  { href: '/about', label: '关于', key: 'about' },
  { href: '/archive', label: '归档', key: 'archive' },
  { href: '/tags', label: '标签', key: 'tags' }
];
const path = current || Astro.url.pathname;
---
<header class="fixed top-0 w-full z-50 bg-[rgb(var(--glass-bg))]/80 dark:bg-surface-dim/80 backdrop-blur-xl border-b border-[rgb(var(--glass-border))]/10 shadow-sm transition-colors">
  <div class="max-w-6xl mx-auto px-6 py-3 flex items-center justify-between">
    <a href="/" class="flex items-center gap-2 group">
      <img
        src={SITE.avatar}
        alt={SITE.author}
        class="w-9 h-9 rounded-full object-cover shadow-sm"
      />
      <span class="font-headline font-bold tracking-tight text-lg text-[rgb(var(--fg))]">
        {SITE.shortTitle}
      </span>
    </a>

    <nav class="flex items-center gap-1 sm:gap-2">
      {navItems.map(item => {
        const active = path === item.href || (item.href !== '/' && path.startsWith(item.href));
        return (
          <a
            href={item.href}
            class={`px-3 py-1.5 rounded-lg text-sm font-body transition-colors ${
              active
                ? 'text-[rgb(var(--primary))] font-bold doodle-underline'
                : 'text-[rgb(var(--fg-soft))] hover:text-[rgb(var(--primary))]'
            }`}
          >
            {item.label}
          </a>
        );
      })}

      <!-- 主题切换 -->
      <button
        id="theme-toggle"
        aria-label="切换主题"
        title="切换主题"
        class="theme-toggle relative w-10 h-10"
      >
        <span class="material-symbols-outlined absolute inset-0 flex items-center justify-center text-[rgb(var(--primary))] transition-all duration-500 dark:rotate-90 dark:opacity-0 rotate-0 opacity-100">light_mode</span>
        <span class="material-symbols-outlined absolute inset-0 flex items-center justify-center text-[rgb(var(--primary))] transition-all duration-500 dark:rotate-0 dark:opacity-100 rotate-90 opacity-0">dark_mode</span>
      </button>
    </nav>
  </div>
</header>

<script is:inline>
  // 主题切换（BaseLayout 已在 <head> 中初始化默认浅色，此处只负责切换交互）
  const themeToggle = document.getElementById('theme-toggle');
  themeToggle?.addEventListener('click', () => {
    document.documentElement.classList.toggle('dark');
    localStorage.setItem(
      'theme',
      document.documentElement.classList.contains('dark') ? 'dark' : 'light'
    );
  });
</script>
